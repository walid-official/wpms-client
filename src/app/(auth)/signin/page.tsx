import { LoginForm } from '@/components/auth/form';
import React from 'react';

const SigninPage = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default SigninPage;